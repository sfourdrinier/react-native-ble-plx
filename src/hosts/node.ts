/**
 * Headless Node host entry (Phase 0 stub).
 */
export class BleManager {
  constructor() {
    throw new Error(
      'unified-ble-manager/node is not implemented yet (Phase 0 stub). Use the root React Native entry for mobile.'
    )
  }
}

export default { BleManager }
