/**
 * Web Bluetooth host entry (Phase 0 stub).
 * Real Web Bluetooth backend lands in a later phase; this export declares the contract.
 */
export class BleManager {
  constructor() {
    throw new Error(
      'unified-ble-manager/web is not implemented yet (Phase 0 stub). Use the root React Native entry for mobile.'
    )
  }
}

export default { BleManager }
